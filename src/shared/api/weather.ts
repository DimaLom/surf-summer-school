import { fetchWithErrorHandling } from './api';

export const getCurrentWeather = async (
  latitude: number,
  longitude: number
) => {
  // Заглушка
};

export const getDailyForecast = async (latitude: number, longitude: number) => {
  // Заглушка
};
